import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cs-vue-router',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
